# Course2Notes — 本機 GPU 轉錄相依（NVIDIA，Windows/Linux）
# 手動安裝：pip install -r requirements-gpu.txt
# ⚠️ macOS 沒有 NVIDIA、也沒有這些 wheel，請改用 requirements-api.txt。
faster-whisper
# CUDA 執行期：光有顯卡驅動不夠，否則 CTranslate2 會 Unable to load libcudnn/libcublas
nvidia-cudnn-cu12
nvidia-cublas-cu12

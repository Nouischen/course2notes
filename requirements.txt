# Course2Notes — 基礎 Python 相依（一律需要）
# 手動安裝：pip install -r requirements.txt（Mac/Linux 用 pip3）
# 下載音訊（download.js 以 python -m yt_dlp 呼叫）
yt-dlp

# 轉錄相依「二選一」，另外裝（技能會依有無 GPU 自動選）：
#   有 NVIDIA GPU（Windows/Linux）：pip install -r requirements-gpu.txt
#   無 GPU / macOS（走 --api）：    pip install -r requirements-api.txt
# 注意：GPU 檔含 nvidia-* wheel，在 macOS 上裝不起來，所以「不要」把它併進本檔。
